# Cart-Abandonment-UI
A lightweight, dependency-free UI component that simulates the “Wait—don’t leave!” modal stores show when you try to close the tab with items still in your cart.   100 % HTML &amp; CSS
